rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to users, transactions, goals, and configs
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
